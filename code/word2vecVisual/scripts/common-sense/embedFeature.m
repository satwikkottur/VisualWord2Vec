function embedding = embedFeature(str, dict, noDims)
    % Function to embed the function using word2vec configuration
    
    % First split it through spaces
    featWords = strsplit(str, ' ');
    
    embedding = zeros(noDims, 1);
    for i = 1:length(featWords)
        if(isKey())
        end
    end
end
